name: TudorPCB

options:
  bundleIdPrefix: net.ptudor
  createIntermediateGroups: true
  developmentLanguage: en
  deploymentTarget:
    iOS: "17.0"
    macOS: "14.0"

packages:
  GerberKit:
    path: GerberKit

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    SWIFT_APPROACHABLE_CONCURRENCY: YES
    SWIFT_DEFAULT_ACTOR_ISOLATION: MainActor
    GENERATE_INFOPLIST_FILE: YES
    SWIFT_EMIT_LOC_STRINGS: NO
    LOCALIZED_STRING_SWIFTUI_SUPPORT: NO
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"
    DEVELOPMENT_TEAM: "55QT38683G"
    CODE_SIGN_STYLE: Automatic
    INFOPLIST_KEY_CFBundleDisplayName: "Tudor PCB"
    INFOPLIST_KEY_LSApplicationCategoryType: "public.app-category.developer-tools"
    INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO
    INFOPLIST_KEY_NSHumanReadableCopyright: "Copyright © 2026 Patrick Tudor. MIT License."

targets:
  TudorPCBMac:
    type: application
    platform: macOS
    sources:
      - path: TudorPCB
        excludes: [Info.plist]
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: net.ptudor.tudorpcb
        PRODUCT_NAME: "Tudor PCB"
        PRODUCT_MODULE_NAME: TudorPCB
        INFOPLIST_FILE: TudorPCB/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: TudorPCB/TudorPCB-macOS.entitlements
        ENABLE_APP_SANDBOX: YES
        ENABLE_HARDENED_RUNTIME: YES
    dependencies:
      - package: GerberKit
    scheme:
      testTargets:
        - TudorPCBTests

  TudorPCB:
    type: application
    platform: iOS
    sources:
      - path: TudorPCB
        excludes: [Info.plist]
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: net.ptudor.tudorpcb
        PRODUCT_NAME: "Tudor PCB"
        PRODUCT_MODULE_NAME: TudorPCB
        INFOPLIST_FILE: TudorPCB/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        TARGETED_DEVICE_FAMILY: "1,2"
        INFOPLIST_KEY_LSSupportsOpeningDocumentsInPlace: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
    dependencies:
      - package: GerberKit

  TudorPCBTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: TudorPCBTests
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: net.ptudor.tudorpcb.tests
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_DEFAULT_ACTOR_ISOLATION: nonisolated
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Tudor PCB.app/Contents/MacOS/Tudor PCB"
        BUNDLE_LOADER: "$(TEST_HOST)"
    dependencies:
      - target: TudorPCBMac

  TudorPCBUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: TudorPCBUITests
        excludes: [Fixtures]
      - path: TudorPCBUITests/Fixtures
        type: folder
        buildPhase: resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: net.ptudor.tudorpcb.uitests
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_DEFAULT_ACTOR_ISOLATION: nonisolated
        TEST_TARGET_NAME: TudorPCB
    dependencies:
      - target: TudorPCB
    scheme:
      testTargets:
        - TudorPCBUITests
