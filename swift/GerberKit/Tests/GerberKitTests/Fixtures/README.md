These synthetic fixtures were authored for this repository's regression tests.
They contain no production/customer fabrication data. Required tests load them from
the test bundle; sibling repositories are optional and report explicit test skips.
Semantic, pixel, security, and app state regressions accompany their respective fixes.
